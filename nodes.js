function dev(a,b){
    if(b==0){
        throw new Error("Cannot divide by zero")
    }
    return a / b;
}

try{
    // dev(4,0)
osama(3,4)}
    catch(e){console.error(e.message)}

/*
2. أنواع الاستثناءات:
أخطاء البرمجة: مثل الوصول إلى عنصر غير موجود في مصفوفة، أو استدعاء دالة غير موجودة.
أخطاء الموارد: مثل فشل الاتصال بقاعدة بيانات أو قراءة ملف غير موجود. "Resource error"
أخطاء منطقية: مثل محاولة تنفيذ عملية غير منطقية (مثل تقسيم رقم على صفر).
*/
                                 